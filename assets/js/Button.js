// $(document).ready(function () {
//   $("[id^='blockButton']").on("click", function () {
//     var userId = $(this).attr("id").replace("blockButton", "")
 
//     // Get the current text of the button
//     var buttonText = $(this).find("span").text();

//     // Toggle the text between "Block" and "Unblock"
//     var newText = buttonText === "Block" ? "Unblock" : "Block";

//     // Update the button text
//     $(this).find("span").text(newText);

//     // Toggle the color between btn-brand and btn-danger
//     $(this).toggleClass("btn-brand btn-danger");
//   });
// });